# Vercel環境変数設定用ファイル
# 
# 注意: 現在の構成では、APIは同一Vercelプロジェクト内の
# /api/ フォルダに配置されているため、環境変数の設定は不要です！
#
# フロントエンドは相対パス（/api/scrape, /api/status）で
# 同一ドメイン内のAPIにアクセスします。
#
# 別途バックエンドをデプロイする必要はありません。
# Vercelが自動的にPythonサーバレス関数として
# api/scrape.py と api/status.py をデプロイします。
#
# このファイルは参考用として残していますが、
# 実際にはVercelに環境変数を設定する必要はありません。
